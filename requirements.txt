# reels-to-email — Python dependencies

# Telegram bot framework
python-telegram-bot>=21.0

# Environment variables
python-dotenv>=1.0.0

# Video download from Instagram
yt-dlp>=2024.10.0

# OpenAI Whisper (transcription)
openai>=3.16.2

# Anthropic Claude (explanation + tagging)
anthropic>=0.40.0

# HTTP for Perplexity API
httpx>=0.27.0

# Resend (email sending)
resend>=2.0.0

# Markdown → HTML (so the LLM's **bold** and ## headings render properly in email)
markdown>=3.6

# HTML sanitizer — LLM/transcript-derived markdown is untrusted; strip unsafe
# tags/attributes before embedding in the email body.
nh3>=0.2.15

# httpx2 — תלות עקיפה של anthropic/openai. הגרסה שנפתרת לבד (2.9.1) נושאת שתי
# פרצות HIGH שנסגרו ב-2.10.0 וב-2.12.0, ושער ה-CI (osv-scanner) חוסם עליהן.
httpx2>=2.12.0
